{% doc %}
  Renders one fixed editorial statistic for the Why choose us section.
{% enddoc %}

<div
  class="why-choose-us__stat"
  role="listitem"
  data-reveal-item
  {{ block.shopify_attributes }}
>
  <p class="why-choose-us__stat-value {% render 'typography-class', preset: block.parent.settings.heading_size, fallback: 'h2' %}">{{ block.settings.value | escape }}</p>
  <div class="why-choose-us__stat-description {% render 'typography-class', preset: block.parent.settings.text_size, fallback: 'xs' %} rte">{{ block.settings.description }}</div>
</div>

{% schema %}
{
  "name": "Why choose us statistic",
  "tag": null,
  "settings": [
    { "type": "text", "id": "value", "label": "Value", "default": "8 MM" },
    { "type": "richtext", "id": "description", "label": "Text", "default": "<p>NATURAL, UNTREATED<br>MOGOK, BURMA</p>" }
  ]
}
{% endschema %}
