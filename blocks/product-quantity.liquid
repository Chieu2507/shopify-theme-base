{% assign product = closest.product | default: product %}
{% assign form_id = 'product-form-' | append: section.id %}
{% render 'quantity-selector', variant: product.selected_or_first_available_variant, block: block, form_id: form_id %}
{% schema %}
{
  "name": "Product quantity",
  "settings": []
}
{% endschema %}
