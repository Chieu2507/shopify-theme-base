{% comment %}Header styles are loaded once from layout/theme.liquid so section re-renders cannot create a stylesheet gap in the Theme Editor.{% endcomment %}

{% liquid
  assign country_form_id = 'HeaderCountrySelector-' | append: section.id
  assign language_form_id = 'HeaderLanguageSelector-' | append: section.id
  assign legacy_logo_width_desktop = section.settings.logo_width_desktop | default: 170
  assign legacy_logo_width_mobile = section.settings.logo_width_mobile | default: 120
  assign logo_block = section.blocks | where: 'type', 'logo' | first
  assign logo_width_desktop = legacy_logo_width_desktop
  assign logo_width_mobile = legacy_logo_width_mobile
  assign logo_block_type = 'image'
  assign logo_block_image = blank
  assign logo_block_transparent_image = blank
  assign logo_block_text = blank
  assign logo_alignment = 'center'
  assign render_logo = false

  if logo_block != blank
    assign render_logo = true
    assign logo_block_type = logo_block.settings.logo_type | default: 'image'
    assign logo_block_image = logo_block.settings.image
    assign logo_block_transparent_image = logo_block.settings.transparent_image
    assign logo_block_text = logo_block.settings.logo_text
    assign logo_alignment = logo_block.settings.alignment | default: 'center'
    assign logo_width_desktop = logo_block.settings.width_desktop | default: 170
    assign logo_width_mobile = logo_block.settings.width_mobile | default: 120

    if logo_width_desktop == 170 and legacy_logo_width_desktop != 170
      assign logo_width_desktop = legacy_logo_width_desktop
    endif

    if logo_width_mobile == 120 and legacy_logo_width_mobile != 120
      assign logo_width_mobile = legacy_logo_width_mobile
    endif
  elsif section.settings.logo_block_migrated != true
    assign render_logo = true
  endif

  if logo_block_type == 'text'
    assign logo_image = blank
    assign logo_image_mobile = blank
    assign logo_image_transparent = blank
    assign logo_image_transparent_mobile = blank
    assign logo_text = logo_block_text | default: shop.name
  else
    assign logo_image = logo_block_image | default: settings.logo | default: settings.logo_mobile
    assign logo_image_mobile = logo_block_image | default: settings.logo_mobile | default: logo_image
    assign logo_image_transparent = logo_block_transparent_image | default: settings.logo_transparent | default: logo_image
    assign logo_image_transparent_mobile = logo_block_transparent_image | default: settings.logo_transparent_mobile | default: settings.logo_transparent | default: logo_image_mobile
    assign logo_text = shop.name
  endif

  assign logo_text_size_desktop = logo_width_desktop | times: 16.0 | divided_by: 170.0 | round: 2
  assign logo_text_size_mobile = logo_width_mobile | times: 14.0 | divided_by: 120.0 | round: 2
  assign navigation_menu = section.settings.menu
  assign menu_links = navigation_menu.links
  assign header_color_scheme = section.settings.color_scheme
  assign account_action = section.settings.account_action | default: 'panel'
  assign account_href = routes.account_url
  assign account_open_new_tab = false
  assign floating_header_enabled = false
  assign transparent_header_enabled = false
  assign has_menu = false
  assign has_mobile_utilities = false
  assign can_change_country = false
  assign show_language = true

  if section.settings.show_language == false
    assign show_language = false
  endif

  if template.name == 'index'
    if section.settings.header_style == 'floating'
      assign floating_header_enabled = true
    elsif section.settings.header_style == 'transparent'
      assign transparent_header_enabled = true
    endif
  endif

  if transparent_header_enabled
    assign header_color_scheme = section.settings.transparent_color_scheme
  endif

  if account_action == 'custom_url' and section.settings.account_custom_url != blank
    assign account_href = section.settings.account_custom_url
  endif

  if account_action == 'new_tab'
    assign account_open_new_tab = true
  endif

  if section.settings.show_localization
    assign has_mobile_utilities = true
  elsif show_language and localization.available_languages.size > 1
    assign has_mobile_utilities = true
  endif

  if localization.available_countries.size > 1
    assign can_change_country = true
  endif

  if menu_links == blank and navigation_menu != blank
    assign navigation_menu_handle = navigation_menu.handle | default: navigation_menu
    assign navigation_menu = linklists[navigation_menu_handle]
    assign menu_links = navigation_menu.links
  endif

  if menu_links != blank
    assign has_menu = true
  endif
%}

<span
  class="header__menu-overlay header__menu-overlay--desktop"
  data-header-menu-overlay="Header-{{ section.id }}"
  aria-hidden="true"
></span>

<header
  id="Header-{{ section.id }}"
  class="header header--logo-{{ section.settings.header_layout }} header--burger-two_stripes_wide header--drawer-mega color-scheme color-{{ header_color_scheme.id }} full-width{% if section.settings.underline_active_link %} header--underline-active{% endif %}{% if section.settings.sticky_behavior != 'none' %} header--sticky{% endif %}{% if template.name != 'index' %} header--bordered{% endif %}{% if transparent_header_enabled %} header--transparent{% endif %}{% if floating_header_enabled %} header--floating{% endif %}"
  style="--header-height: {{ section.settings.height_desktop }}px; --header-height-mobile: {{ section.settings.height_mobile }}px;"
  data-sticky-behavior="{{ section.settings.sticky_behavior }}"
  data-mega-menu-animation="slide_down"
  data-mega-menu-animation-duration="300"
  data-floating-header="{{ floating_header_enabled }}"
  data-transparent-header="{{ transparent_header_enabled }}"
  data-default-color-class="color-{{ section.settings.color_scheme.id }}"
  data-transparent-color-class="color-{{ section.settings.transparent_color_scheme.id }}"
  data-deferred-module="{{ 'section-header.js' | asset_url }}"
  data-header
>
  <span class="header__mega-background" aria-hidden="true"></span>
  <div class="header__inner">
    {% if has_menu or has_mobile_utilities %}
      <details class="header__menu-disclosure" data-open-label="{{ 'header.open_menu' | t | escape }}" data-close-label="{{ 'header.close_menu' | t | escape }}">
        <summary class="header__menu-toggle" aria-label="{{ 'header.open_menu' | t | escape }}" aria-expanded="false" aria-controls="HeaderMobileDrawer-{{ section.id }}">
          <span class="header__menu-icon header__menu-icon--open header__burger-lines" aria-hidden="true">
            <span></span>
            <span></span>
            <span></span>
          </span>
        </summary>
      </details>

      <div
        id="HeaderMobileDrawer-{{ section.id }}"
        class="header__mobile-drawer"
        data-header-mobile-drawer
      >
        <span
          class="header__menu-overlay header__menu-overlay--mobile"
          data-header-menu-overlay="Header-{{ section.id }}"
          aria-hidden="true"
        ></span>
        <div class="header__mobile-drawer-surface color-scheme color-{{ section.settings.color_scheme.id }}">
          <div class="header__mobile-drawer-header">
            <span id="HeaderMobileDrawerTitle-{{ section.id }}" class="visually-hidden">{{ 'header.primary_navigation' | t }}</span>
            {% if render_logo %}
              <a class="header__mobile-drawer-logo" href="{{ routes.root_url }}" aria-label="{{ logo_text | escape }}" style="--header-logo-width-mobile: {{ logo_width_mobile }}px; --header-logo-text-size-mobile: {{ logo_text_size_mobile }}px;">
                {% if logo_image_mobile != blank %}
                  {{ logo_image_mobile | image_url: width: 360 | image_tag: widths: '120, 180, 240, 360', sizes: '(max-width: 959px) var(--header-logo-width-mobile), 120px', alt: shop.name, loading: 'eager' }}
                {% else %}
                  <span class="header__logo-text">{{ logo_text | escape }}</span>
                {% endif %}
              </a>
            {% endif %}
            <button class="header__mobile-drawer-close modal-close-button close-icon" type="button" aria-label="{{ 'header.close_menu' | t | escape }}" data-header-mobile-close></button>
          </div>

          {% render 'header-navigation',
            section: section,
            menu_links: menu_links,
            navigation_variant: 'mobile',
            has_menu: has_menu,
            can_change_country: can_change_country,
            account_href: account_href,
            account_open_new_tab: account_open_new_tab,
            account_action: account_action
          %}
        </div>
      </div>
    {% endif %}

    {% if has_menu %}
      {% render 'header-navigation',
        section: section,
        menu_links: menu_links,
        navigation_variant: 'desktop',
        has_menu: has_menu,
        can_change_country: can_change_country,
        account_href: account_href,
        account_open_new_tab: account_open_new_tab
      %}
    {% endif %}

    {% if render_logo %}
      <div
        class="header__logo header__logo--align-{{ logo_alignment }}"
        style="--header-logo-width: {{ logo_width_desktop }}px; --header-logo-width-mobile: {{ logo_width_mobile }}px; --header-logo-text-size: {{ logo_text_size_desktop }}px; --header-logo-text-size-mobile: {{ logo_text_size_mobile }}px;"
        {% if logo_block != blank %}{{ logo_block.shopify_attributes }}{% endif %}
      >
          <a href="{{ routes.root_url }}" aria-label="{{ logo_text | escape }}">
            {% if logo_image != blank %}
              <picture class="header__logo-image header__logo-image--dark">
                {% if logo_image_mobile != blank %}<source media="(max-width: 959px)" srcset="{{ logo_image_mobile | image_url: width: 360 }}">{% endif %}
                {{ logo_image | image_url: width: 600 | image_tag: widths: '120, 180, 240, 360, 480, 600', sizes: '(max-width: 959px) var(--header-logo-width-mobile), var(--header-logo-width)', alt: logo_image.alt | default: shop.name, loading: 'eager' }}
              </picture>
              <picture class="header__logo-image header__logo-image--light">
                {% if logo_image_transparent_mobile != blank %}<source media="(max-width: 959px)" srcset="{{ logo_image_transparent_mobile | image_url: width: 360 }}">{% endif %}
                {{ logo_image_transparent | image_url: width: 600 | image_tag: widths: '120, 180, 240, 360, 480, 600', sizes: '(max-width: 959px) var(--header-logo-width-mobile), var(--header-logo-width)', alt: logo_image_transparent.alt | default: shop.name, loading: 'eager' }}
              </picture>
            {% else %}
              <span class="header__logo-text">{{ logo_text | escape }}</span>
            {% endif %}
          </a>
      </div>
    {% endif %}

    <div class="header__actions">
      {% if section.settings.show_localization %}
        <div class="header__localization">
          {% if can_change_country %}
            <details class="header__localization-selector{% if section.settings.dropdown_trigger == 'hover' %} header__localization-selector--hover{% endif %}">
              <summary aria-label="{{ 'header.country_region' | t | escape }}">
                {% if section.settings.show_country_flag %}
                  {{ localization.country | image_url: width: 32 | image_tag: class: 'header__localization-flag', widths: '16, 32', alt: localization.country.name, loading: 'lazy' }}
                {% endif %}
                <span class="header__localization-label">{{ localization.country.currency.iso_code | escape }} {{ localization.country.currency.symbol | escape }}</span>
                <span class="header__dropdown-icon header__dropdown-icon--chevron" aria-hidden="true">{% render 'icon', name: 'chevron-down' %}</span>
              </summary>
              <div class="header__localization-popover">
                {% form 'localization', id: country_form_id %}
                  <div class="header__localization-select" data-header-country-picker>
                    <input type="hidden" name="country_code" value="{{ localization.country.iso_code }}" data-header-country-input>
                    <div id="{{ country_form_id }}-Options" class="header__localization-options" role="listbox" aria-label="{{ 'header.country_region' | t | escape }}">
                      {% for country in localization.available_countries %}
                        <button class="header__localization-option{% if country.iso_code == localization.country.iso_code %} header__localization-option--selected{% endif %}" type="button" role="option" aria-selected="{% if country.iso_code == localization.country.iso_code %}true{% else %}false{% endif %}" data-header-country-option data-country-code="{{ country.iso_code }}">
                          {{ country | image_url: width: 32 | image_tag: class: 'header__localization-option-flag', widths: '16, 32', alt: '', loading: 'lazy' }}
                          <span>{{ country.name | escape }} ({{ country.currency.iso_code | escape }} {{ country.currency.symbol | escape }})</span>
                          {% if country.iso_code == localization.country.iso_code %}<span class="header__localization-option-check" aria-hidden="true">✓</span>{% endif %}
                        </button>
                      {% endfor %}
                    </div>
                  </div>
                  <noscript>
                    <select name="country_code">
                      {% for country in localization.available_countries %}
                        <option value="{{ country.iso_code }}"{% if country.iso_code == localization.country.iso_code %} selected{% endif %}>{{ country.name | escape }} ({{ country.currency.iso_code | escape }} {{ country.currency.symbol | escape }})</option>
                      {% endfor %}
                    </select>
                    <button type="submit">{{ 'header.apply' | t }}</button>
                  </noscript>
                {% endform %}
              </div>
            </details>
          {% else %}
            <span class="header__localization-current" aria-label="{{ 'header.country_region' | t | escape }}">
              {% if section.settings.show_country_flag %}
                {{ localization.country | image_url: width: 32 | image_tag: class: 'header__localization-flag', widths: '16, 32', alt: localization.country.name, loading: 'lazy' }}
              {% endif %}
              <span class="header__localization-label">{{ localization.country.currency.iso_code | escape }} {{ localization.country.currency.symbol | escape }}</span>
            </span>
          {% endif %}
        </div>
      {% endif %}

      {% if show_language and localization.available_languages.size > 1 %}
        <div class="header__localization header__language-localization">
          <details class="header__localization-selector header__language-selector{% if section.settings.dropdown_trigger == 'hover' %} header__localization-selector--hover{% endif %}">
            <summary aria-label="{{ 'header.language' | t | escape }}: {{ localization.language.endonym_name | default: localization.language.name | escape }}">
              <span class="header__localization-label">{{ localization.language.endonym_name | default: localization.language.name | escape }}</span>
              <span class="header__dropdown-icon header__dropdown-icon--chevron" aria-hidden="true">{% render 'icon', name: 'chevron-down' %}</span>
            </summary>
            <div class="header__localization-popover">
              {% form 'localization', id: language_form_id %}
                <div class="header__localization-select" data-header-language-picker>
                  <input type="hidden" name="language_code" value="{{ localization.language.iso_code }}" data-header-language-input>
                  <div id="{{ language_form_id }}-Options" class="header__localization-options" role="listbox" aria-label="{{ 'header.language' | t | escape }}">
                    {% for language in localization.available_languages %}
                      <button class="header__localization-option{% if language.iso_code == localization.language.iso_code %} header__localization-option--selected{% endif %}" type="button" role="option" aria-selected="{% if language.iso_code == localization.language.iso_code %}true{% else %}false{% endif %}" data-header-language-option data-language-code="{{ language.iso_code }}">
                        <span>{{ language.endonym_name | default: language.name | escape }}</span>
                        {% if language.iso_code == localization.language.iso_code %}<span class="header__localization-option-check" aria-hidden="true">✓</span>{% endif %}
                      </button>
                    {% endfor %}
                  </div>
                </div>
                <noscript>
                  <select name="language_code">
                    {% for language in localization.available_languages %}
                      <option value="{{ language.iso_code }}"{% if language.iso_code == localization.language.iso_code %} selected{% endif %}>{{ language.endonym_name | default: language.name | escape }}</option>
                    {% endfor %}
                  </select>
                  <button type="submit">{{ 'header.apply' | t }}</button>
                </noscript>
              {% endform %}
            </div>
          </details>
        </div>
      {% endif %}

      {% if section.settings.show_search %}
        <a class="header__action header__search-trigger" href="{{ routes.search_url }}" aria-label="{{ 'header.search' | t | escape }}" aria-haspopup="dialog" aria-controls="HeaderSearch-{{ section.id }}" data-header-search-open>
          {% render 'icon', name: 'search' %}
        </a>
      {% endif %}

      {% if section.settings.show_account and shop.customer_accounts_enabled %}
        {% if account_action == 'panel' %}
          <shopify-account class="header__action header__action--account"{% if section.settings.customer_account_menu != blank and section.settings.customer_account_menu != 'customer-account-main-menu' %} menu="{{ section.settings.customer_account_menu }}"{% endif %}>
            <span slot="signed-out-avatar" class="header__account-avatar" aria-hidden="true">{% render 'icon', name: 'account' %}</span>
          </shopify-account>
        {% else %}
          <a class="header__action header__action--account" href="{{ account_href }}" aria-label="{{ 'header.login' | t | escape }}"{% if account_open_new_tab %} target="_blank" rel="noopener"{% endif %}>
            {% render 'icon', name: 'account' %}
          </a>
        {% endif %}
      {% endif %}

      {% if section.settings.show_cart %}
        <a class="header__action header__cart" href="{{ routes.cart_url }}" aria-label="{{ 'header.cart' | t | escape }}" data-cart-count-label="{{ 'header.cart_count' | t: count: '__count__' | escape }}"{% if settings.cart_action == 'drawer' %} data-cart-drawer-open aria-controls="CartDrawer" aria-expanded="false"{% endif %}>
          {% render 'icon', name: 'bag' %}
          {% if cart.item_count > 0 %}
            <span class="header__cart-count" aria-label="{{ 'header.cart_count' | t: count: cart.item_count | escape }}">
              {{ cart.item_count }}
            </span>
          {% endif %}
        </a>
      {% endif %}
    </div>
  </div>

  {% if section.settings.show_search %}
    <dialog scroll-lock data-scroll-lock-owner="search" class="header-search-modal color-scheme color-{{ section.settings.color_scheme.id }}" id="HeaderSearch-{{ section.id }}" aria-labelledby="HeaderSearchLabel-{{ section.id }}" data-header-search-modal data-predictive-search-url="{{ routes.predictive_search_url }}" data-currency-code="{{ localization.country.currency.iso_code | default: shop.currency | escape }}" data-no-products-text="{{ 'search.no_products' | t | escape }}" data-no-results-text="{{ 'search.no_results' | t: term: '__term__' | escape }}" data-product-count-one="{{ 'search.product_count.one' | t: count: '__count__' | escape }}" data-product-count-other="{{ 'search.product_count.other' | t: count: '__count__' | escape }}">
      <div class="header-search-modal__inner">
        <button class="header-search-modal__close modal-close-button close-icon" type="button" aria-label="{{ 'header.close_search' | t | escape }}" data-header-search-close></button>
        <form class="header-search-modal__form search-field" action="{{ routes.search_url }}" method="get" role="search" data-header-search-form>
          <label class="visually-hidden" id="HeaderSearchLabel-{{ section.id }}" for="HeaderSearchInput-{{ section.id }}">{{ 'search.label' | t }}</label>
          <input id="HeaderSearchInput-{{ section.id }}" class="header-search-modal__input search-field__input" type="search" name="q" placeholder="{{ 'search.placeholder' | t }}" autocomplete="off" role="combobox" aria-autocomplete="list" aria-haspopup="listbox" aria-expanded="false" aria-controls="HeaderSearchPanel-{{ section.id }}-products" data-header-search-input>
          <button class="header-search-modal__clear search-field__clear" type="button" aria-label="{{ 'search.clear_search' | t | escape }}" data-header-search-clear hidden>
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="m7 7 10 10M17 7 7 17" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
          </button>
          <button class="header-search-modal__submit search-field__submit" type="submit" aria-label="{{ 'search.submit' | t | escape }}">
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><circle cx="10.8" cy="10.8" r="6.7" fill="none" stroke="currentColor" stroke-width="1.5"/><path d="m16 16 4.4 4.4" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
          </button>
        </form>

        <div class="header-search-modal__predictive" data-header-search-predictive aria-live="polite" aria-atomic="false" hidden>
          <div class="header-search-modal__tabs" role="tablist" aria-label="{{ 'search.result_tabs' | t | escape }}">
            <button id="HeaderSearchTab-{{ section.id }}-products" type="button" role="tab" aria-selected="true" aria-controls="HeaderSearchPanel-{{ section.id }}-products" tabindex="0" data-header-search-tab="products">{{ 'search.products' | t }}</button>
            <button id="HeaderSearchTab-{{ section.id }}-collections" type="button" role="tab" aria-selected="false" aria-controls="HeaderSearchPanel-{{ section.id }}-collections" tabindex="-1" data-header-search-tab="collections">{{ 'search.collections' | t }}</button>
          </div>
          <div class="header-search-modal__panels">
            <div id="HeaderSearchPanel-{{ section.id }}-products" role="tabpanel" aria-labelledby="HeaderSearchTab-{{ section.id }}-products" data-header-search-panel="products"></div>
            <div id="HeaderSearchPanel-{{ section.id }}-collections" role="tabpanel" aria-labelledby="HeaderSearchTab-{{ section.id }}-collections" hidden data-header-search-panel="collections"></div>
            <p class="header-search-modal__empty" data-header-search-empty hidden></p>
          </div>
          <a class="button button--primary header-search-modal__view-all" href="{{ routes.search_url }}" data-header-search-view-all>{{ 'search.view_all_results' | t }}</a>
        </div>
      </div>
    </dialog>
  {% endif %}
</header>




{% schema %}
{
  "name": "Header",
  "max_blocks": 16,
  "enabled_on": {
    "groups": [
      "header"
    ]
  },
  "settings": [
    {
      "type": "header",
      "content": "General"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "main-menu"
    },
    {
      "type": "select",
      "id": "dropdown_trigger",
      "label": "Desktop dropdown trigger",
      "info": "Mobile navigation always opens with a tap.",
      "options": [
        { "value": "click", "label": "Click" },
        { "value": "hover", "label": "Hover" }
      ],
      "default": "hover"
    },
    {
      "type": "checkbox",
      "id": "underline_active_link",
      "label": "Underline active and hovered links",
      "default": false
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "header_layout",
      "label": "Desktop logo position",
      "info": "Controls the Header grid. Use the Logo block alignment setting to align the logo within this position.",
      "options": [
        { "value": "left", "label": "Logo left" },
        { "value": "center", "label": "Logo center" }
      ],
      "default": "center"
    },
    {
      "type": "select",
      "id": "header_style",
      "label": "Header style",
      "info": "Floating and transparent styles apply on the homepage only.",
      "options": [
        { "value": "none", "label": "Normal" },
        { "value": "floating", "label": "Floating" },
        { "value": "transparent", "label": "Transparent" }
      ],
      "default": "none"
    },
    {
      "type": "select",
      "id": "sticky_behavior",
      "label": "Sticky header",
      "info": "On mobile, a sticky header hides while scrolling down and reveals when scrolling up.",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "always",
          "label": "Always"
        }
      ],
      "default": "none"
    },
    { "type": "range", "id": "height_desktop", "min": 64, "max": 112, "step": 2, "unit": "px", "label": "Desktop height", "default": 84 },
    { "type": "range", "id": "height_mobile", "min": 52, "max": 84, "step": 2, "unit": "px", "label": "Mobile height", "default": 60 },
    { "type": "range", "id": "logo_width_desktop", "min": 100, "max": 260, "step": 5, "unit": "px", "label": "Legacy desktop logo width", "default": 170, "visible_if": "{{ false }}" },
    { "type": "range", "id": "logo_width_mobile", "min": 80, "max": 180, "step": 5, "unit": "px", "label": "Legacy mobile logo width", "default": 120, "visible_if": "{{ false }}" },
    { "type": "checkbox", "id": "logo_block_migrated", "label": "Legacy logo block migration marker", "default": false, "visible_if": "{{ false }}" },
    { "type": "header", "content": "Actions" },
    { "type": "checkbox", "id": "show_search", "label": "Show search", "default": true },
    { "type": "checkbox", "id": "show_localization", "label": "Show country and currency", "default": true },
    { "type": "checkbox", "id": "show_language", "label": "Show language", "default": true },
    { "type": "checkbox", "id": "show_country_flag", "label": "Show country flag", "default": true, "visible_if": "{{ section.settings.show_localization }}" },
    { "type": "checkbox", "id": "show_account", "label": "Show account", "default": true },
    {
      "type": "select",
      "id": "account_action",
      "label": "Account icon action",
      "visible_if": "{{ section.settings.show_account }}",
      "options": [
        { "value": "panel", "label": "Open account panel" },
        { "value": "redirect", "label": "Redirect to account page" },
        { "value": "new_tab", "label": "Open account page in new tab" },
        { "value": "custom_url", "label": "Open custom URL" }
      ],
      "default": "panel"
    },
    {
      "type": "url",
      "id": "account_custom_url",
      "label": "Custom URL",
      "visible_if": "{{ section.settings.show_account and section.settings.account_action == 'custom_url' }}"
    },
    { "type": "checkbox", "id": "show_cart", "label": "Show cart", "default": true },
    {
      "type": "header",
      "content": "Customer account"
    },
    {
      "type": "paragraph",
      "content": "Customer accounts must be enabled in Shopify admin. The account panel uses the selected menu on desktop and mobile."
    },
    {
      "type": "link_list",
      "id": "customer_account_menu",
      "label": "Menu",
      "info": "Leave the standard menu selected to use Shopify's built-in account links, or choose a custom menu when needed.",
      "default": "customer-account-main-menu",
      "visible_if": "{{ section.settings.show_account and section.settings.account_action == 'panel' }}"
    },
    {
      "type": "header",
      "content": "Appearance"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Default / scrolled color scheme",
      "info": "Used by the normal header, sticky surface, mobile drawer, and search dialog.",
      "default": "scheme-1"
    },
    {
      "type": "color_scheme",
      "id": "transparent_color_scheme",
      "label": "Overlay color scheme",
      "info": "Used while the header sits over homepage hero content.",
      "default": "scheme-3",
      "visible_if": "{{ section.settings.header_style == 'transparent' }}"
    }
  ],
  "blocks": [
    {
      "type": "logo",
      "name": "Logo",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "Use this block to control the Header logo. Empty image fields fall back to Theme settings > Brand."
        },
        {
          "type": "radio",
          "id": "logo_type",
          "label": "Logo type",
          "options": [
            { "value": "image", "label": "Image" },
            { "value": "text", "label": "Text" }
          ],
          "default": "image"
        },
        {
          "type": "header",
          "content": "Image logo",
          "visible_if": "{{ block.settings.logo_type == 'image' }}"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image",
          "info": "Optional Header-only override. Applies on desktop and mobile, then falls back to the Brand logo.",
          "visible_if": "{{ block.settings.logo_type == 'image' }}"
        },
        {
          "type": "image_picker",
          "id": "transparent_image",
          "label": "Image for transparent header",
          "info": "Optional Header-only override. Displays when the Header is transparent, then falls back to the Brand asset.",
          "visible_if": "{{ block.settings.logo_type == 'image' }}"
        },
        {
          "type": "text",
          "id": "logo_text",
          "label": "Text logo",
          "info": "Leave blank to use the shop name.",
          "visible_if": "{{ block.settings.logo_type == 'text' }}"
        },
        {
          "type": "header",
          "content": "Logo sizing"
        },
        { "type": "range", "id": "width_desktop", "min": 100, "max": 260, "step": 5, "unit": "px", "label": "Desktop logo width", "default": 170 },
        { "type": "range", "id": "width_mobile", "min": 80, "max": 180, "step": 5, "unit": "px", "label": "Mobile logo width", "default": 120 },
        {
          "type": "header",
          "content": "Element alignment in block"
        },
        {
          "type": "select",
          "id": "alignment",
          "label": "Horizontal alignment",
          "info": "Aligns the logo inside its current Header position. The position itself is controlled in Header > Layout.",
          "options": [
            { "value": "left", "label": "Left" },
            { "value": "center", "label": "Center" },
            { "value": "right", "label": "Right" }
          ],
          "default": "center"
        }
      ]
    },
    {
      "type": "menu_badge",
      "name": "Menu badge",
      "settings": [
        {
          "type": "header",
          "content": "Menu badge"
        },
        {
          "type": "text",
          "id": "menu_item",
          "label": "Menu item title",
          "info": "Match a top-level menu item title. Matching ignores letter case; use one badge block per item."
        },
        {
          "type": "text",
          "id": "text",
          "label": "Badge text",
          "default": "New"
        }
      ]
    },
    {
      "type": "mega_menu",
      "name": "Mega menu",
      "settings": [
        {
          "type": "header",
          "content": "Mega menu"
        },
        {
          "type": "text",
          "id": "menu_item",
          "label": "Top-level menu item",
          "info": "Enter one or more top-level menu item titles separated by semicolons. Matching ignores letter case; each item must have child links."
        },
        {
          "type": "select",
          "id": "text_size",
          "label": "Text size",
          "options": [
            { "value": "small", "label": "Small" },
            { "value": "medium", "label": "Medium" },
            { "value": "large", "label": "Large" }
          ],
          "default": "medium"
        },
        {
          "type": "text",
          "id": "column_heading",
          "label": "Column heading",
          "info": "Optional heading above the navigation columns."
        },
        {
          "type": "select",
          "id": "columns",
          "label": "Desktop columns",
          "options": [
            {
              "value": "2",
              "label": "2"
            },
            {
              "value": "3",
              "label": "3"
            },
            {
              "value": "4",
              "label": "4"
            }
          ],
          "default": "3"
        },
        {
          "type": "header",
          "content": "Promotion 1"
        },
        {
          "type": "image_picker",
          "id": "promotion_1_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "promotion_1_heading",
          "label": "Heading"
        },
        {
          "type": "select",
          "id": "promotion_1_heading_size",
          "label": "Heading size",
          "options": [
            { "value": "h1", "label": "XX-large" },
            { "value": "h2", "label": "X-large" },
            { "value": "h3", "label": "Large" },
            { "value": "h4", "label": "Medium" },
            { "value": "h5", "label": "Small" },
            { "value": "h6", "label": "X-small" }
          ],
          "default": "h4"
        },
        {
          "type": "select",
          "id": "promotion_1_heading_tag",
          "label": "HTML Tag",
          "info": "Use H2–H6 for menu content; H1 is reserved for the page heading.",
          "options": [
            { "value": "h2", "label": "H2" },
            { "value": "h3", "label": "H3" },
            { "value": "h4", "label": "H4" },
            { "value": "h5", "label": "H5" },
            { "value": "h6", "label": "H6" }
          ],
          "default": "h3"
        },
        {
          "type": "richtext",
          "id": "promotion_1_text",
          "label": "Text"
        },
        {
          "type": "select",
          "id": "promotion_1_text_size",
          "label": "Text size",
          "options": [
            { "value": "x-large", "label": "X-large" },
            { "value": "large", "label": "Large" },
            { "value": "medium", "label": "Medium" },
            { "value": "small", "label": "Small" },
            { "value": "x-small", "label": "X-small" }
          ],
          "default": "medium"
        },
        {
          "type": "text_alignment",
          "id": "promotion_1_text_alignment",
          "label": "Alignment",
          "default": "left"
        },
        {
          "type": "text",
          "id": "promotion_1_link_label",
          "label": "Link label",
          "info": "The CTA renders only when both label and link are filled."
        },
        {
          "type": "url",
          "id": "promotion_1_link",
          "label": "Link"
        },
        {
          "type": "header",
          "content": "Promotion 2"
        },
        {
          "type": "image_picker",
          "id": "promotion_2_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "promotion_2_heading",
          "label": "Heading"
        },
        {
          "type": "select",
          "id": "promotion_2_heading_size",
          "label": "Heading size",
          "options": [
            { "value": "h1", "label": "XX-large" },
            { "value": "h2", "label": "X-large" },
            { "value": "h3", "label": "Large" },
            { "value": "h4", "label": "Medium" },
            { "value": "h5", "label": "Small" },
            { "value": "h6", "label": "X-small" }
          ],
          "default": "h4"
        },
        {
          "type": "select",
          "id": "promotion_2_heading_tag",
          "label": "HTML Tag",
          "info": "Use H2–H6 for menu content; H1 is reserved for the page heading.",
          "options": [
            { "value": "h2", "label": "H2" },
            { "value": "h3", "label": "H3" },
            { "value": "h4", "label": "H4" },
            { "value": "h5", "label": "H5" },
            { "value": "h6", "label": "H6" }
          ],
          "default": "h3"
        },
        {
          "type": "richtext",
          "id": "promotion_2_text",
          "label": "Text"
        },
        {
          "type": "select",
          "id": "promotion_2_text_size",
          "label": "Text size",
          "options": [
            { "value": "x-large", "label": "X-large" },
            { "value": "large", "label": "Large" },
            { "value": "medium", "label": "Medium" },
            { "value": "small", "label": "Small" },
            { "value": "x-small", "label": "X-small" }
          ],
          "default": "medium"
        },
        {
          "type": "text_alignment",
          "id": "promotion_2_text_alignment",
          "label": "Alignment",
          "default": "left"
        },
        {
          "type": "text",
          "id": "promotion_2_link_label",
          "label": "Link label",
          "info": "The CTA renders only when both label and link are filled."
        },
        {
          "type": "url",
          "id": "promotion_2_link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Header",
      "category": "Theme layout",
      "settings": {
        "logo_block_migrated": true
      },
      "blocks": [
        {
          "type": "logo",
          "settings": {
            "logo_type": "image",
            "width_desktop": 170,
            "width_mobile": 120,
            "alignment": "center"
          }
        },
        {
          "type": "mega_menu",
          "settings": {
            "menu_item": "Collections",
            "text_size": "medium",
            "column_heading": "",
            "columns": "3",
            "promotion_1_heading": "New Arrivals",
            "promotion_1_heading_size": "h4",
            "promotion_1_heading_tag": "h3",
            "promotion_1_text": "<p>Discover the latest pieces, selected for everyday use and lasting style.</p>",
            "promotion_1_text_size": "medium",
            "promotion_1_text_alignment": "left",
            "promotion_1_link_label": "SHOP NEW ARRIVALS",
            "promotion_1_link": "/collections/new-arrivals",
            "promotion_2_heading": "Best Sellers",
            "promotion_2_heading_size": "h4",
            "promotion_2_heading_tag": "h3",
            "promotion_2_text": "<p>Explore customer favorites loved for their exceptional craftsmanship and lasting beauty.</p>",
            "promotion_2_text_size": "medium",
            "promotion_2_text_alignment": "left",
            "promotion_2_link_label": "SHOP BEST SELLERS",
            "promotion_2_link": "/collections/best-sellers"
          }
        }
      ]
    }
  ]
}
{% endschema %}
