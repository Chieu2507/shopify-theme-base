{% doc %}
  Renders a product quantity input.

  @param {variant} variant
  @param {block} block
  @param {string} [form_id]
{% enddoc %}
{% assign quantity_min = variant.quantity_rule.min | default: 1 %}
<div class="quantity-selector" {{ block.shopify_attributes }} data-quantity-selector>
  <label for="Quantity-{{ section.id }}">{{ 'quantity.label' | t }}</label>
  <div class="quantity-selector__control">
    <button type="button" data-quantity-decrease aria-label="{{ 'quantity.decrease' | t | escape }}">−</button>
    <input id="Quantity-{{ section.id }}" type="number" name="quantity" value="{{ quantity_min }}" min="{{ quantity_min }}"{% if variant.quantity_rule.max != blank %} max="{{ variant.quantity_rule.max }}"{% endif %} step="{{ variant.quantity_rule.increment | default: 1 }}" inputmode="numeric" aria-label="{{ 'quantity.label' | t | escape }}"{% if form_id != blank %} form="{{ form_id }}"{% endif %} data-quantity-input>
    <button type="button" data-quantity-increase aria-label="{{ 'quantity.increase' | t | escape }}">+</button>
  </div>
</div>
